input {
  tcp {
    port => 5000
    codec => json_lines
  }
}

filter {
  # فیلتر برای پردازش لاگ‌ها
  mutate {
    add_field => { "service" => "%{[fields][service]}" }
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "logs-%{+YYYY.MM.dd}-%{service}"
  }
}